import { Router } from "express";

const routes = Router();

routes.post("/login", () => "A definir");
routes.post("/logout", () => "A definir");
routes.post("/esqueci-senha", () => "A definir");

export default routes;
